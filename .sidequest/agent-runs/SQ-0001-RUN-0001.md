---
schema: 1
quest_id: SQ-0001
run_id: RUN-0001
agent: codex
language: ko
status: completed
started_at: 2026-08-02T09:33:19.067Z
updated_at: 2026-08-02T18:37:04+09:00
quest_file: .sidequest/quests/SQ-0001.md
---

# 작업 목표 (Objective)

SQ-0001 작업 진행 내용을 기록합니다.

# 작업 설명 (Work Summary)

로그인 callback이 오래된 `127.0.0.1:3000`으로 돌아가지 않고 현재 개발/배포 origin을 사용하도록 auth redirect 경계를 점검하고, 개발 서버/browser test origin을 `localhost:5173`으로 일치시킨다.

# 구현 상세 (Implementation Details)

Quest 명세와 관련 `.codex`/`docs` 규칙을 읽었다. 현재 작업 트리에는 사용자가 진행 중인 backend auth 전환 및 `localhost:5173` 기준 변경이 이미 존재하므로 이를 보존한다. API 계약에 따라 Google auth의 `returnTo`는 host/port를 직접 저장하지 않는 상대 경로로 유지하고, Vite/Playwright의 로컬 browser origin만 `localhost:5173`으로 통일한다.

# 진행 상황 (Progress)

- [x] Quest 명세 읽기
- [x] 요구사항 구현
- [x] 관련 검증 실행

# 변경 파일 (Changed Files)

- `moodi/vite.config.ts`와 `moodi/playwright.config.ts`의 browser origin 기준은 기존 사용자 변경대로 `http://localhost:5173`을 유지했다.
- `moodi/src/features/auth/services/authGoogleService.ts`는 callback `returnTo`에 host/port를 저장하지 않고 현재 pathname/query/hash만 전달한다.
- `docs/architecture/flow.md`의 오래된 `127.0.0.1:3000` 참조를 `http://localhost:5173`으로 갱신했다.
- 배포 환경에서는 `VITE_API_BASE_URL`을 통해 API origin을 주입하고, 인증 callback은 상대 경로를 사용하므로 localhost를 배포 주소로 고정하지 않는다.

# 검증 (Validation)

- `npm run typecheck` 통과.
- `npm run lint` 통과.
- `npm test` 통과: 8개 파일, 45개 테스트.
- `npm run build` 통과: Vite production build 완료.

# Token Usage

- Input: 추정 11,500 tokens
- Output: 추정 2,800 tokens
- Total: 추정 14,300 tokens

# 남은 작업 (Remaining)

- 구현 및 검증 완료. 사용자 검토 대기.
