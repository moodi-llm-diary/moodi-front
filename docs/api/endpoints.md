# 엔드포인트 목록 - moodi

이 문서는 목록 전용이다. Request/Response 상세를 작성하지 않는다.

현재 프론트엔드에 계약되거나 구현된 HTTP endpoint는 없다. Diary는 비동기 Repository contract의 localStorage adapter, 분석은 `local-rule-mock`, 인증은 비밀정보가 없는 mock profile로만 동작한다.

백엔드 계약이 확정되기 전에는 endpoint를 추정해 추가하지 않는다.
